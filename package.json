{
  "name": "all-notes",
  "version": "0.0.1",
  "engines": {
    "node": "10.15.3"
  },
  "scripts": {
    "build": "cd api && npm install && npm run build && npm pack && cd ../fe-all-notes && npm install && npm run build && cd ../be-all-notes && npm install",
    "start": "cd be-all-notes && npm run start:prod"
  }
}
